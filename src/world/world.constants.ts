// Food
export const FOOD_ITEM_RADIUS = 3;
export const FOOD_CLUSTER_SPACING = 7;
export const MAX_FOOD_QUANTITY = 5;

// Obstacles
export const OBSTACLE_DEFAULT_SIZE = 40;
// Distance an ant is pushed beyond an obstacle edge when it ends up inside one
// (e.g. an obstacle placed on top of it).
export const OBSTACLE_ESCAPE_MARGIN = 2;

// Quadtree
export const QUADTREE_MAX_DEPTH = 7;
